{
  "name": "hexapod-gait-lab",
  "version": "0.1.0",
  "private": true,
  "description": "Dev tooling for the Hexapod Gait Lab dashboard. The app itself is dependency-free; Playwright is only used by test/smoke.mjs to drive the built page in a real browser.",
  "scripts": {
    "build": "./build.sh",
    "smoke": "node test/smoke.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
