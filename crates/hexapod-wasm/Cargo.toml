[package]
name = "hexapod-wasm"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "C-ABI bridge exposing the hexapod core to the browser dashboard."

[lib]
name = "hexapod_wasm"
path = "src/lib.rs"
crate-type = ["cdylib", "rlib"]

[dependencies]
hexapod-core = { path = "../hexapod-core", features = ["rapier"] }

[lints]
workspace = true
